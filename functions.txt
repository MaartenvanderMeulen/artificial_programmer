(
    # list functions here
    #(function fac (n) (if (gt n 1) (mul n (fac (sub n 1))) 1))
    #(function range (n) (for i n i))
    (function sum (list)
            (last
                (assign result 0)
                (for elem list (assign result (add result elem)))
                result
            )
    )
    # layer 1
    #(function get_col (board col) (for row board (at row col)))
    #(function get_diag1 (board) (for i (len board) (at board i i)))
    #(function get_diag2 (board) (for i (len board) (at board i (sub (len board) 1 i))))
    #(function are_all_equal_to (values x) (eq (sum (for value values (eq value x))) (len values)))
    #(function get_magic_number_n (n) (div (mul n (add (mul n n) 1)) 2))
    # layer 2
    #(function get_row_sums (board) (for row board (sum row )))
    #(function get_col_sums (board) (for i (len board) (sum (get_col board i))))
    #(function get_diag_sums (board) (list2 (sum (get_diag1 board)) (sum (get_diag2 board))))
    #(function get_magic_number (board) (get_magic_number_n (len board)))    
    # layer 3
    #(function get_sums_magic_square (board)
    #    (add (get_row_sums board) (add (get_col_sums board) (get_diag_sums board)))
    #)
    # layer 4
    #(function is_magic_square (board)
    #    (are_all_equal_to (get_sums_magic_square board) (get_magic_number board))
    #)
)
#    (function get_col (board col) (for board board (at (for board board board) col)))
#    (function get_diag1 (board) (for i (len board) (at board i i)))
#    (function get_diag2 (board) (for i 3 (sub (at board i (sub (at i board i) (sub i 2))) (for 3 2 (for (for 2 board 2) (for i board board) (for 3 3 2))))))
#    (function get_magic_number_n (n) (div (mul (mul 1 (add (div 2 2) (mul n n))) n) 2))
#    (function are_all_equal_to (values x) (eq values (for values values x)))
#    (function get_row_sums (board) (for board (for row board (sum row)) board))
#    (function get_col_sums (board) (get_col (for i 3 (for (for board board i) board (sum (get_col board i)))) (for (sum i) (for board i board) (get_col board i))))
#    (function get_diag_sums (board) (list (sum (get_diag1 board)) (sum (get_diag2 board))))
#    (function get_magic_number (board) (get_magic_number_n (len board)))
#    (function get_sums_magic_square (board) (add (add (get_row_sums board) (get_col_sums board)) (get_diag_sums board)))
#    (function is_magic_square (board) (are_all_equal_to (get_sums_magic_square board) (get_magic_number board)))
#    (function get_col (board col) (for board board (at (for board board board) col)))
#    (function get_diag1 (board) (for i (len board) (at board i i)))
#    (function get_diag2 (board) (for i 3 (sub (at board i (sub (at i board i) (sub i 2))) (for 3 2 (for (for 2 board 2) (for i board board) (for 3 3 2))))))
#    (function get_magic_number_n (n) (div (mul (mul 1 (add (div 2 2) (mul n n))) n) 2))
#    (function are_all_equal_to (values x) (eq values (for values values x)))
#    (function get_row_sums (board) (for board (for row board (sum row)) board))
#    (function get_col_sums (board) (get_col (for i 3 (for (for board board i) board (sum (get_col board i)))) (for (sum i) (for board i board) (get_col board i))))
#    (function get_diag_sums (board) (list (sum (get_diag1 board)) (sum (get_diag2 board))))
#    (function get_magic_number (board) (get_magic_number_n (len board)))
#    (function get_sums_magic_square (board) (add (add (get_row_sums board) (get_col_sums board)) (get_diag_sums board)))
#    (function is_magic_square (board) (are_all_equal_to (get_sums_magic_square board) (get_magic_number board)))
#    (function get_col (board col) (for board board (at board col)))
#    (function get_diag1 (board) (for i (len board) (at board i i)))
#    (function get_diag2 (board) (for i 3 (at board i (sub 2 i))))
#    (function get_magic_number_n (n) (div (mul (add (add 1 (div 1 n)) (mul n n)) (div n 1)) 2))
#    (function are_all_equal_to (values x) (len (for (for x x value) (eq values (for (for value values values) values x)) (for value x values))))
#    (function get_row_sums (board) (for board (for row board row) (sum board)))
#    (function get_col_sums (board) (for i (sum (sum 3)) (sum (get_col board i))))
#    (function get_diag_sums (board) (list (sum (get_diag1 board)) (sum (get_diag2 board))))
#    (function get_magic_number (board) (get_magic_number_n (len board)))
#    (function get_sums_magic_square (board) (add (get_row_sums board) (add (get_col_sums board) (get_diag_sums board))))
#    (function is_magic_square (board) (are_all_equal_to (get_sums_magic_square board) (get_magic_number board)))
#    (function get_col (board col) (for board board (at board col)))
#    (function get_diag1 (board) (for i (len board) (at board i i)))
#    (function get_diag2 (board) (for i 3 (at board i (sub (sub 2 i) (for 3 2 3)))))
#    (function get_magic_number_n (n) (div (mul (add (div 1 1) (mul n n)) (mul n 1)) 2))
#    (function are_all_equal_to (values x) (eq values (for value values x)))
#    (function get_row_sums (board) (for board board (sum board)))
#    (function get_col_sums (board) (for i 3 (sum (get_col board i))))
#    (function get_diag_sums (board) (list (sum (get_diag1 board)) (sum (get_diag2 board))))
#    (function get_magic_number (board) (get_magic_number_n (len board)))
#    (function get_sums_magic_square (board) (add (add (get_row_sums board) (get_col_sums board)) (get_diag_sums board)))
#    (function is_magic_square (board) (are_all_equal_to (get_sums_magic_square board) (get_magic_number board)))
#    (function get_col (board col) (for row board (at row col)))
#    (function get_diag1 (board) (for i (len board) (at board i i)))
#    (function get_diag2 (board) (for i 3 (at board i (sub 2 i))))
#    (function get_magic_number_n (n) (div (mul (add (mul 1 1) (mul n n)) n) 2))
#    (function are_all_equal_to (values x) (eq (for (sum (for x values values)) values x) values))
#    (function get_row_sums (board) (for row board (sum (for row row row))))
#    (function get_col_sums (board) (for i 3 (sum (get_col board i))))
#    (function get_diag_sums (board) (list (sum (get_diag1 board)) (sum (get_diag2 board))))
#    (function get_magic_number (board) (get_magic_number_n (len board)))
#    (function get_sums_magic_square (board) (add (add (get_row_sums board) (get_col_sums board)) (get_diag_sums board)))
#    (function is_magic_square (board) (are_all_equal_to (get_sums_magic_square board) (get_magic_number board)))
#    (function get_col (board col) (for board board (at (for board board board) col)))
#    (function get_diag1 (board) (for i (len board) (at board i i)))
#    (function get_diag2 (board) (for i 3 (sub (at board i (sub (at i board i) (sub i 2))) (for 3 2 (for (for 2 board 2) (for i board board) (for 3 3 2))))))
#    (function get_magic_number_n (n) (div (mul (mul 1 (add (div 2 2) (mul n n))) n) 2))
#    (function are_all_equal_to (values x) (eq values (for values values x)))
#    (function get_row_sums (board) (for board (for row board (sum row)) board))
#    (function get_col_sums (board) (get_col (for i 3 (for (for board board i) board (sum (get_col board i)))) (for (sum i) (for board i board) (get_col board i))))
#    (function get_diag_sums (board) (list (sum (get_diag1 board)) (sum (get_diag2 board))))
#    (function get_magic_number (board) (get_magic_number_n (len board)))
